@model Cinema

@{
    ViewData["Title"] = "Update Cinema";
 }

<div class="row text">
    <div class="col-md-8 offset-md-2">
        <p class="">
            <h3>Update Cinema Details</h3>
        </p>
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger"> </div>
                
                <!--for show picture of Url onchange though js code below-->
                    <div class="form-group text-center">
                        <img class="border-dark rounded-circle" style="max-width:150px" id="CinemaLogoPreview" />
                    </div>
                <div class="form-group">
                    <label asp-for="CinemaLogo" class="control-label"></label>
                    <input asp-for="CinemaLogo" class="form-control"/>
                    <span asp-validation-for="CinemaLogo" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Name" class="control-label"></label>
                    <input asp-for="Name" class="form-control"/>
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Description" class="control-label"></label>
                    <textarea asp-for="Description" class="form-control" rows="4">@Model.Description</textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-outline-success float-right" value="Update"/>
                    <a class="btn btn-outline-info" asp-action="Index">Cinema List</a>
                </div>
                    </form>
            </div>
        </div>
    </div>
</div>

@section scripts{ 
    <script>
        //if something wrong happened to form then it will notshow picture in form so to prevent from that
        $(document).ready(function () {
            var output = document.getElementById("CinemaLogoPreview");
            output.src = $("#CinemaLogo").val();
        })
        //for showing image in form
        $("#CinemaLogo").on("change", function () {
            var output = document.getElementById("CinemaLogoPreview");
            output.src = $(this).val();
        })
    </script>

}